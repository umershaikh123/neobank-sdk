// Re-export core types for convenience so consumers don't need to import from both packages
export type {
  NeobankConfig,
  Vault,
  User,
  Portfolio,
  PortfolioPosition,
  TransactionPayload,
  TransactionStep,
  TransactionSummary,
  TransactionPayloadRequest
} from '@raga-neobank/core';
